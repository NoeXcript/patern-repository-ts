# REST API NodeJS Express DB IN MEMORY

> To install the run de bellow command

```sh
$ npm install
```

> After install all dependencies to execute the run the follow command in terminal

```sh
 npm run dev
````


# Dex.JS 💻💻
